<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.retrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activities.DeletePostActivity"></activity>
        <activity android:name=".ui.activities.PutPostActivity" />
        <activity android:name=".ui.activities.ManyCommentsActivity" />
        <activity android:name=".ui.activities.ManyPostsActivity" />
        <activity android:name=".ui.activities.CreatePostActivity" />
        <activity android:name=".ui.activities.OnePostDynamicallyActivity" />
        <activity android:name=".ui.activities.UserPostsActivity" />
        <activity android:name=".ui.activities.OnePostActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>